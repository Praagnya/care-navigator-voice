# Version constants for prompts, tools, and data pipeline.
# Increment these when the corresponding artifact changes so that
# Langfuse traces and evaluation runs can be correlated to the exact
# version of the prompt or schema that produced them.

PROMPT_VERSION = "v1"
TOOL_SCHEMA_VERSION = "v1"
PIPELINE_VERSION = "0.1.0"
